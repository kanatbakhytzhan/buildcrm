# 🚀 ИНСТРУКЦИЯ ПО ДЕПЛОЮ НА VERCEL

## ✅ ЧТО УЖЕ ГОТОВО

1. ✅ API URL изменен на продакшн: `https://crm-api-5vso.onrender.com`
2. ✅ Добавлен скрипт сборки: `npm run build`
3. ✅ Установлены зависимости для веба: `react-dom`, `react-native-web`
4. ✅ Настроен `app.json` (секция `web`)
5. ✅ Создан `vercel.json` (конфигурация)
6. ✅ Обновлен `.gitignore`

---

## 📋 ШАГИ ДЛЯ ДЕПЛОЯ

### ШАГ 1: УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
npm install --legacy-peer-deps
```

Флаг `--legacy-peer-deps` нужен для React 19 совместимости.

---

### ШАГ 2: ТЕСТОВАЯ СБОРКА (ОПЦИОНАЛЬНО)

Протестируйте сборку локально перед отправкой на Vercel:

```bash
npm run build
```

Если команда выполнится без ошибок и создастся папка `dist/` - все готово!

---

### ШАГ 3: СОЗДАНИЕ FAVICON (ВАЖНО!)

**Проблема**: В `app.json` указан путь `./assets/favicon.png`, но файла может не быть.

**Решение**:

1. Создайте иконку 192x192px (синий фон, белая буква **B** или логотип)
2. Сохраните её как `assets/favicon.png`
3. Или используйте онлайн-генератор: https://favicon.io/favicon-generator/

**Временное решение**: Если некогда делать иконку, удалите строку из `app.json`:

```json
// Убрать эту строку:
"favicon": "./assets/favicon.png",
```

---

### ШАГ 4: ОТПРАВКА НА GITHUB

```bash
# Инициализация репозитория (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "feat: BuildCRM PWA готов к деплою"

# Переименование ветки в main (если нужно)
git branch -M main

# Подключение удаленного репозитория (ЗАМЕНИТЕ НА СВОЙ URL!)
git remote add origin https://github.com/ваш-username/buildcrm.git

# Отправка на GitHub
git push -u origin main
```

---

### ШАГ 5: ДЕПЛОЙ НА VERCEL

#### Вариант A: Через веб-интерфейс (Проще)

1. Зайдите на https://vercel.com
2. Авторизуйтесь через GitHub
3. Нажмите **"New Project"**
4. Выберите ваш репозиторий `buildcrm`
5. Vercel автоматически обнаружит настройки из `vercel.json`
6. Проверьте:
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
7. Нажмите **"Deploy"**
8. Ждите 2-3 минуты
9. Готово! Vercel выдаст URL: `https://buildcrm-xxx.vercel.app`

#### Вариант B: Через CLI (Для продвинутых)

```bash
# Установка Vercel CLI
npm install -g vercel

# Логин
vercel login

# Деплой (следуйте инструкциям)
vercel

# Для продакшн деплоя
vercel --prod
```

---

## 🧪 ПРОВЕРКА ПОСЛЕ ДЕПЛОЯ

1. Откройте выданный URL в браузере
2. Должна загрузиться страница логина
3. Введите тестовые данные:
   - **Email**: `kanat@skai.kz`
   - **Пароль**: `Kanaezz15!`
4. Проверьте, что:
   - ✅ Логин работает
   - ✅ Список заявок загружается
   - ✅ Детали заявки открываются
   - ✅ Кнопки "Позвонить" / "WhatsApp" работают
   - ✅ Можно отметить заявку как "Успешно"

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### Проблема 1: "Module not found: react-dom"

**Причина**: Не установлены веб-зависимости.

**Решение**:
```bash
npm install react-dom react-native-web --legacy-peer-deps
git add package.json package-lock.json
git commit -m "fix: add web dependencies"
git push
```

Vercel автоматически пересоберет проект.

---

### Проблема 2: "Failed to compile"

**Причина**: Ошибка в коде или несовместимость пакетов.

**Решение**:
1. Проверьте логи в Vercel Dashboard
2. Найдите строку с ошибкой
3. Исправьте локально
4. Закоммитьте и запушьте снова

---

### Проблема 3: "API request failed / Network Error"

**Причина**: CORS или неправильный API URL.

**Решение**:
1. Убедитесь, что бэкенд на Render работает
2. Проверьте `services/api.ts` - должно быть `https://crm-api-5vso.onrender.com`
3. Проверьте, что на бэкенде включен CORS для вашего домена

---

### Проблема 4: Белый экран

**Причина**: Неправильная маршрутизация.

**Решение**:
1. Проверьте, что `vercel.json` содержит:
   ```json
   "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
   ```
2. Если проблема сохраняется, откройте DevTools (F12) и проверьте Console

---

## 🎉 ГОТОВО!

Ваше приложение теперь доступно онлайн как PWA!

**Преимущества PWA:**
- ✅ Можно добавить на главный экран телефона
- ✅ Работает офлайн (с кешированием)
- ✅ Быстрая загрузка
- ✅ Не нужен App Store / Google Play

**Ваш Vercel URL**: `https://buildcrm-xxx.vercel.app`

Поделитесь ссылкой с клиентами! 🚀
