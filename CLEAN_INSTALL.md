# 🧹 Чистая переустановка зависимостей

## ✅ Изменения в package.json

- **React**: `19.1.0` → `18.2.0`
- **React DOM**: `19.1.0` → `18.2.0`
- **react-native-web**: `~0.19.13` → `~0.19.10`
- **Expo**: `~54.0.0` → `~51.0.0` (совместим с React 18)
- **React Native**: `0.81.5` → `0.74.5` (для Expo SDK 51)
- **expo-router**, **expo-status-bar**, **expo-secure-store**, **expo-linking** — приведены к версиям для SDK 51
- **@types/react**: `~19.1.0` → `~18.2.0`

Цель: убрать конфликт peer dependencies и обеспечить проход `npm install` без ошибок.

---

## 🚀 Что сделать перед установкой

### 1. Удалить lock-файл и node_modules

**Windows (PowerShell):**
```powershell
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json
```

**Или вручную:**
- Удалите папку `node_modules`
- Удалите файл `package-lock.json`

### 2. Установить зависимости заново

```bash
npm install
```

Без `--legacy-peer-deps` — дерево зависимостей должно собираться без конфликтов.

### 3. (Опционально) Проверить совместимость Expo

```bash
npx expo install --fix
```

Подставит рекомендуемые версии под выбранный Expo SDK.

---

## 📦 После установки

- Запуск: `npm start` или `npm run web`
- Сборка для Vercel: `npm run build`

Если появятся новые ошибки (например, из-за API expo-router 3.x vs 6.x), их можно будет поправить по логам.
