# ⚡ БЫСТРЫЙ ДЕПЛОЙ НА VERCEL

## 📝 ЧЕКЛИСТ ГОТОВНОСТИ

- [x] ✅ API URL обновлен на `https://crm-api-5vso.onrender.com`
- [x] ✅ Скрипт сборки `npm run build` добавлен
- [x] ✅ `react-dom` и `react-native-web` добавлены в зависимости
- [x] ✅ `app.json` настроен для веба
- [x] ✅ `vercel.json` создан
- [x] ✅ `.gitignore` обновлен

## 🚀 КОМАНДЫ ДЛЯ КОПИПАСТА

### 1️⃣ УСТАНОВКА ЗАВИСИМОСТЕЙ

```bash
npm install --legacy-peer-deps
```

### 2️⃣ ТЕСТ СБОРКИ (ОПЦИОНАЛЬНО)

```bash
npm run build
```

Должна создаться папка `dist/`. Если есть - отлично!

### 3️⃣ ОТПРАВКА НА GITHUB

**ЗАМЕНИТЕ `ваш-username` и `buildcrm` на ваши данные!**

```bash
git init
git add .
git commit -m "feat: BuildCRM PWA ready for production"
git branch -M main
git remote add origin https://github.com/ваш-username/buildcrm.git
git push -u origin main
```

### 4️⃣ ДЕПЛОЙ НА VERCEL

Зайдите на https://vercel.com

1. **Авторизуйтесь** через GitHub
2. Нажмите **"New Project"**
3. Выберите ваш репозиторий
4. Настройки должны определиться автоматически:
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`
5. Нажмите **"Deploy"**
6. Ждите 2-3 минуты ☕

## ✅ ПРОВЕРКА

После деплоя:

1. Откройте выданный URL
2. Логин: `kanat@skai.kz`
3. Пароль: `Kanaezz15!`
4. Проверьте все функции

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

Читайте подробную инструкцию в `DEPLOY.md`

---

**Важно**: Если бэкенд на Render "засыпает" (cold start), первая загрузка может занять 30-60 секунд.
